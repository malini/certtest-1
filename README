DEPENDENCIES

marionette_client
tornado


RUNNING TESTS

Execute `python -m semiauto test_sms` to run the test_sms test class.
To automatically detect available tests you can run `pytohon -m
semiauto discover` to recursively look for them.